  <%= form_with model: @article do |a|%>
  <%= a.label :title %>
  <%= a.text_field :title, class: 'form-control mb-3' %>
  <%= a.label :content %>
  <%= a.text_area :content, class: 'form-control' %>
  <%= a.submit %>

  <% end %>
